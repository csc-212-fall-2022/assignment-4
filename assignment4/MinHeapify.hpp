#include <vector>

namespace assignment4 {
std::vector<int> MinHeapify(std::vector<int> vec, int idx);

int Left(int idx);

int Right(int idx);

bool IsMinHeap(std::vector<int>);
} // namespace assignment4